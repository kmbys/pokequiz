<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>pokequiz</title>
<link rel="stylesheet" href="/static/index.css">
</head>
<body>
	<p>ステージ {{ stage.current }}</p>
	<p>このポケモンは？<br>{{ quiz.question }}</p>
	{% for option in quiz.options %}
		<input type="radio" name="option" id="{{ option.name }}" class="{{ option.class_name }}">
		<label class="option_label" for="{{ option.name }}">{{ option.name }}</label>
	{% endfor %}
	<div id="result_correct">
		<p>せいかい！</p>
		<img src="{{ quiz.answer_image }}">
		<form action="/{{ stage.next }}" method="get">	
			<input type="submit" value="つぎへすすむ" class="submit">
		</form>
	</div>
	<div id="result_wrong">
		<p>ざんねん！せいかいは<strong> {{ quiz.answer_name }} </strong>でした。</p>
		<img src="{{ quiz.answer_image }}">
		<form action="/1" method="get">	
			<input type="submit" value="さいしょから" class="submit">
		</form>
	</div>
</body>
</html>
